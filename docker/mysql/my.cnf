[mysqld]
# 文字コードをutf8mb4に統一する（絵文字や日本語を正しく保存するため）
character-set-server = utf8mb4
collation-server = utf8mb4_general_ci

# タイムゾーンを日本時間にする
default-time-zone = '+09:00'

[client]
default-character-set = utf8mb4
